{{>layouts/header}}
    <div>
        {{# id}}
            <h1>{{id}}{{username}}</h1>
        {{/id}}
        {{^ id}}
            <h1>{{username}}</h1>
        {{/id}}
    </div>
{{>layouts/footer}}